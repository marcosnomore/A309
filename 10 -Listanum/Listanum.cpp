#include "Listanum.h"
#include <iostream>
/*
using namespace std;

template<class T>
void Listanum<T>::add_elem(T dato)
{
	if(nElem<MAX_ELEM)
		lista[nElem++]=dato;
}

template<class T>
T Listanum<T>::get_elem(int pos)
{
	if(pos<MAX_ELEM)
		return lista[pos];
}

template<class T>
int Listanum<T>::size()
{
	return nElem;
}

template<class T>
T Listanum<T>::pop(void)
{
	nElem--;
	return lista[nElem];
}

template<class T>
ostream & Listanum<T>::print(ostream & o)
{
	o<<"[";
	for(int i=0; i<nElem; i++)
	{
		o<<lista[i]<<" ";
	}
	o<<"]"<<" ["<<nElem<<"]";
	return o;
}
*/
/*		Primera opción. Segunda opción en .h
//predeclaración de tipos
template Listanum<double>;
template Listanum<int>;
template Listanum<float>;
*/
